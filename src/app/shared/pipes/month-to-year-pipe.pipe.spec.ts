import { MonthToYearPipePipe } from './month-to-year-pipe.pipe';

describe('MonthToYearPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MonthToYearPipePipe();
    expect(pipe).toBeTruthy();
  });
});
