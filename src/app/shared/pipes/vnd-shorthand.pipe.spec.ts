import { VndShorthandPipe } from './vnd-shorthand.pipe';

describe('VndShorthandPipe', () => {
  it('create an instance', () => {
    const pipe = new VndShorthandPipe();
    expect(pipe).toBeTruthy();
  });
});
