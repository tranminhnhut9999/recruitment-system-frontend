import { ApplicationStateLabelPipe } from './application-state-label.pipe';

describe('ApplicationStateLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicationStateLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
